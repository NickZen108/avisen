name: '[RETIRED] V3 model bakeoff'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The 65-call model bakeoff is retired after model selection.'
          echo 'It is intentionally inert so it cannot bypass the 10 DKK/day newsroom budget guard.'

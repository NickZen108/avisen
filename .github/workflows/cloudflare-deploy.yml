name: '[RETIRED] Legacy Cloudflare v1 deploy'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy v1 runtime is retired
        run: |
          echo 'Legacy Cloudflare v1 worker morgentidende has been retired.'
          echo 'This workflow intentionally does not deploy anything.'
          echo 'Current Pipeline v3 lives in NickZen108/morgentidende and must not be changed from this repo.'

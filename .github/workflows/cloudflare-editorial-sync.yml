name: '[QUARANTINED] Pipeline v2 editorial sync'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantined:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline v2 is quarantined
        run: |
          echo 'Pipeline v2 is quarantined and cannot run editorial production.'
          echo 'Do not reactivate this workflow. Build Pipeline v3 separately.'

name: '[QUARANTINED] Pipeline v2 burst'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantined:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline v2 is quarantined
        run: |
          echo 'Pipeline v2 burst production is quarantined.'
          echo 'No editorial cycles will be executed.'

# Scheduled (legacy queue)

Nye strukturerede artikler bruger `status: ready|published` i `content/articles/` og får først `published_at`, når de faktisk går live.

Denne mappe bevares for legacy-noter. Et klokkeslæt her må aldrig bruges som falsk publiceringstid. Se `SCHEDULE.md`.

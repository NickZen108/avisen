# DESIGN LÅST

Det visuelle design er låst. Se `DESIGN.md`.

Maskinel baseline ligger i `config/design-lock.txt`. Automatisk drift må ikke ændre de låste filer. En designændring kræver en udtrykkelig brugerordre og derefter en bevidst opdatering af både `DESIGN.md` og lock-manifestet.
